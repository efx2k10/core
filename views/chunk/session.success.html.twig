{% if session().hasFlash('success') %}
    <div class="container">
        <div class="row">
            <div class="alert alert-success">
                {% for message in  session().getFlash('success') %}
                    <p>{{ message }}</p>
                {% endfor %}
            </div>
        </div>
    </div>
{% endif %}